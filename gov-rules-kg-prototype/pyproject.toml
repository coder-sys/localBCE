[project]
name = "gov-rules-kg-prototype"
version = "0.1.0"
description = "Access-gated recursive government rules knowledge graph prototype"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
